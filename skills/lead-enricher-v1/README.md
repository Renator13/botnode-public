# lead-enricher-v1

BotNode skill service and worker for `lead_enricher_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
lead-enricher-api
```

Run worker:

```bash
lead-enricher-worker
```

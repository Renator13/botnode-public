# sentiment-analyzer-v1

BotNode skill service and worker for `sentiment_analyzer_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
sentiment-analyzer-api
```

Run worker:

```bash
sentiment-analyzer-worker
```

# key-point-extractor-v1

BotNode skill service and worker for `key_point_extractor_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
key-point-extractor-api
```

Run worker:

```bash
key-point-extractor-worker
```

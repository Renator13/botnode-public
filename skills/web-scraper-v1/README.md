# web-scraper-v1

BotNode skill service and worker for `web_scraper_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
web-scraper-api
```

Run worker:

```bash
web-scraper-worker
```

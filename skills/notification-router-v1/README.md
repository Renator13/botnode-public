# notification-router-v1

BotNode skill service and worker for `notification_router_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
notification-router-api
```

Run worker:

```bash
notification-router-worker
```

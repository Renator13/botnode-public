# scheduler-v1

BotNode skill service and worker for `scheduler_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
scheduler-api
```

Run worker:

```bash
scheduler-worker
```

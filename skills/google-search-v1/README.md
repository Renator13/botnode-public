# google-search-v1

BotNode skill service and worker for `google_search_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
google-search-api
```

Run worker:

```bash
google-search-worker
```

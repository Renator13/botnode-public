# pdf-reader-v1

BotNode skill service and worker for `pdf_reader_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
pdf-reader-api
```

Run worker:

```bash
pdf-reader-worker
```

# csv-parser-v1

BotNode skill service and worker for `csv_parser_v1`.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

Run API:

```bash
csv-parser-api
```

Run worker:

```bash
csv-parser-worker
```
